# 02 — UI Design

> ⬜ **Placeholder — chưa triển khai.**

Giai đoạn thiết kế UI theo tài liệu PRD từ `01-research-and-prd/`. AI đóng vai Designer. Output của giai đoạn này (design file/link) sẽ là input cho `03-dev-code/` (điền vào `project-template.md`).

## Skill ứng viên (chưa gắn, chỉ ghi chú tham khảo)

Đang có sẵn ở `.agents/skills/` (Pj3, local) — **chưa vendor vào toolkit**, vì role này chưa được thiết kế qua brainstorming riêng. Khi thật sự bắt tay xây role này, cân nhắc:

- `frontend-design`, `baseline-ui`, `create-design-md`, `improve-ui` — hướng dẫn thiết kế/dọn UI, nhưng phần lớn ví dụ nghiêng về web — cần lọc/kiểm chứng lại cho đúng bối cảnh mobile trước khi dùng.
- `sleek-design-mobile-apps` — SaaS thiết kế mobile trả phí (sleek.design), **không** thuộc toolkit mặc định (xem lý do loại trừ ở `03-dev-code/tech-stacks/react-native-expo.md`) — chỉ cân nhắc nếu chủ động muốn trả phí dùng công cụ này.
