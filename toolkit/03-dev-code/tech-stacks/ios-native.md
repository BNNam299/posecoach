# Tech Stack Profile: iOS Native

> ⬜ **Placeholder — chưa triển khai.**

Sẽ điền khi thực sự dùng đến. Ghi chú kỹ thuật quan trọng cần nhớ khi viết file này:

- Bắt buộc cần **máy Mac + Xcode** (hoặc Mac cloud như MacinCloud) — ràng buộc "chỉ dùng Windows" của profile Expo **không áp dụng** cho profile này.
- Ngôn ngữ dự kiến: Swift/SwiftUI.
- Cần xác định lại: kiến trúc (MVVM?), cấu trúc thư mục chuẩn, quy tắc thư viện (SPM vs CocoaPods).
