# Reusable Modules: React Native + Expo

> ⬜ **Placeholder — Nhóm C, thu hẹp lại sau khi cài plugin `expo@claude-plugins-official`.**

Plugin Expo chính thức (skill `expo-router`, `expo-design-system`, `expo-data-fetching`...) đã bao phủ navigation pattern, design tokens, và data fetching bằng nguồn chính thức tự cập nhật — **không cần tự viết lại các phần đó ở đây nữa**.

- `state-management.md` — ✅ đã giải quyết bằng skill `react-native-patterns` (`affaan-m/ECC`, đã kiểm chứng thật/uy tín). Chỉ còn phần thật sự cá nhân hoá (Zustand vs Jotai, convention cấu trúc store) là tuỳ chọn, không bắt buộc phải điền ngay.

Toàn bộ Nhóm C của profile React Native + Expo coi như đã xong ở mức "đủ dùng" — chỉ còn tinh chỉnh theo sở thích riêng nếu muốn.
