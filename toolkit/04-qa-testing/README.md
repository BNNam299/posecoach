# 04 — QA & Testing

> ⬜ **Placeholder — chưa triển khai.**

Giai đoạn kiểm thử sản phẩm sau khi `03-dev-code/` hoàn thành. AI đóng vai Tester.

## Skill ứng viên (chưa gắn, chỉ ghi chú tham khảo)

- `code-review-expert` — đã vendor sẵn trong `03-dev-code/SKILL/_shared/` (dùng chung với role Dev ở Phase 4 của cả 2 build-sequence (`react-native-expo.md` và `android-native.md`)), role QA có thể tái sử dụng luôn, không cần cài thêm.
- `.agents/skills/fixing-accessibility/` (Pj3, local, chưa vendor) — kiểm tra khả năng tiếp cận (accessibility), nhưng ví dụ nghiêng về web (ARIA, HTML) — cần kiểm chứng lại cho mobile trước khi dùng.
